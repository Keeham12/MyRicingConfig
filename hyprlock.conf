
background {
    monitor =
    path = /home/Orion/Pictures/Wallpapers/9220932.png  
    #path = /home/Orion/Pictures/Wallpapers/9220942.jpg
    #path = /home/Orion/Pictures/Wallpapers/futurecity.jpg
    #path = /home/Orion/Pictures/Wallpapers/fcity.jpg
   

    # all these options are taken from hyprland, see https://wiki.hyprland.org/Configuring/Variables/#blur for explanations
    blur_size = 4
    blur_passes = 1 # 0 disables blurring
    noise = 0.0117
    contrast = 1.3000 # Vibrant!!!
    brightness = 0.8000
    vibrancy = 0.2100
    vibrancy_darkness = 0.0
}




#=======================================BORDERS================================================================
label {
    monitor =
    text = ╭─────────────────────────────────────────╮
    color = rgba(235, 0, 0, 0.4)
    font_size = 16
    font_family = JetBrains Mono
    position = 0, 280
    halign = center
    valign = center
}

label {
    monitor =
    text = ╰─────────────────────────────────────────╯
    color = rgba(255, 0, 0, 0.4)
    font_size = 16
    font_family = JetBrains Mono
    position = 0, -280
    halign = center
    valign = center
}
#=========================================================================================================
#========================================TIME==============================================================
# Time glow effect
label {
    monitor =
    text = $TIME
    color = rgba(224, 62, 62, 0.3)
    font_size = 85
    font_family = $font
    position = 2, 152
    halign = center
    valign = center
}

# Time display
label {
    monitor =
    text = $TIME
    color = $text
    font_size = 80
    font_family = $font
    position = 0, 150
    halign = center
    valign = center
    shadow_passes = 3
    shadow_size = 8
    shadow_color =rgba(224, 62, 62, 0.3)
}

label {
    monitor =
    text = cmd[update:3600000] date +"%A, %d %B %Y"
    color = $subtext1
    font_size = 20
    font_family = $font
    position = 0, 80
    halign = center
    valign = center
    shadow_passes = 1
    shadow_size = 2
    shadow_color = rgba(0, 0, 0, 0.6)
}


#=================================================================================================
input-field {
    monitor =
    size = 280, 45
    outline_thickness = 2
    dots_size = 0.25
    dots_spacing = 0.3
    dots_center = true
    dots_rounding = -1
    outer_color = $accent
    inner_color = $surface0
    font_color = $text
    fade_on_empty = false
    fade_timeout = 1000
    placeholder_text = Password...
    hide_input = false
    check_color = $green
    fail_color = $red
    fail_text = Authentication Failed! ($ATTEMPTS)
    capslock_color = $yellow
    position = 0, -140
    halign = center
    valign = center
    shadow_passes = 4
    shadow_size = 10
    shadow_color = rgba(238, 35, 8, 0.4)
}
# ==============================================================================
# WEATHER
# ==============================================================================

label {
    monitor =
    text = cmd[update:600000] timeout 8s bash -c 'city="Berlin, Germany"; UA="hyprlock-weather/1.0 (fkeeham12@gmail.com)"; coords=$(curl -sS -A "$UA" "https://nominatim.openstreetmap.org/search?format=json&q=${city// /+}" | jq -r ".[0] | .lat+\",\"+.lon" 2>/dev/null); [ -z "$coords" ] && { echo "🌤️ --°"; exit; }; lat=${coords%%,*}; lon=${coords##*,}; weather_data=$(curl -sS --max-time 6 "https://api.open-meteo.com/v1/forecast?latitude=$lat&longitude=$lon&current_weather=true&hourly=temperature_2m,relative_humidity_2m,apparent_temperature,wind_speed_10m,wind_direction_10m&temperature_unit=celsius&wind_speed_unit=kmh&timezone=Europe%2FBerlin&forecast_days=1"); [ -z "$weather_data" ] && { echo "🌤️ --°"; exit; }; temp=$(echo "$weather_data" | jq -r ".current_weather.temperature // empty" | cut -d. -f1); code=$(echo "$weather_data" | jq -r ".current_weather.weathercode // empty"); wind_speed=$(echo "$weather_data" | jq -r ".current_weather.windspeed // empty" | cut -d. -f1); wind_dir=$(echo "$weather_data" | jq -r ".current_weather.winddirection // empty"); ctime=$(echo "$weather_data" | jq -r ".current_weather.time // empty"); idx=-1; if [ -n "$ctime" ]; then idx=$(echo "$weather_data" | jq -r --arg t "$ctime" ".hourly.time | index($t) // -1"); fi; humidity=""; feels_like=""; if [ "$idx" -ge 0 ]; then humidity=$(echo "$weather_data" | jq -r ".hourly.relative_humidity_2m[$idx] // empty"); feels_like=$(echo "$weather_data" | jq -r ".hourly.apparent_temperature[$idx] // empty" | cut -d. -f1); fi; if [ -n "$temp" ] && [ -n "$code" ]; then case "$code" in 0) icon="☀️";; 1|2|3) icon="🌤️";; 45|48) icon="🌫️";; 51|53|55|56|57) icon="🌦️";; 61|63|65|66|67) icon="🌧️";; 71|73|75|77) icon="❄️";; 80|81|82) icon="🌧️";; 85|86) icon="❄️";; 95|96|99) icon="⛈️";; *) icon="🌤️";; esac; wind_arrow=""; if [ -n "$wind_dir" ]; then d=$(printf "%.0f" "$wind_dir" 2>/dev/null || echo "$wind_dir"); if [ "$d" -ge 337 ] || [ "$d" -lt 23 ]; then wind_arrow="↑"; elif [ "$d" -ge 23 ] && [ "$d" -lt 68 ]; then wind_arrow="↗"; elif [ "$d" -ge 68 ] && [ "$d" -lt 113 ]; then wind_arrow="→"; elif [ "$d" -ge 113 ] && [ "$d" -lt 158 ]; then wind_arrow="↘"; elif [ "$d" -ge 158 ] && [ "$d" -lt 203 ]; then wind_arrow="↓"; elif [ "$d" -ge 203 ] && [ "$d" -lt 248 ]; then wind_arrow="↙"; elif [ "$d" -ge 248 ] && [ "$d" -lt 293 ]; then wind_arrow="←"; elif [ "$d" -ge 293 ] && [ "$d" -lt 337 ]; then wind_arrow="↖"; fi; fi; echo "${icon} ${temp}°C"; [ -n "$feels_like" ] && [ "$feels_like" != "$temp" ] && echo "Feels ${feels_like}°C"; [ -n "$wind_speed" ] && [ -n "$wind_arrow" ] && echo "💨 ${wind_speed}km/h ${wind_arrow}"; [ -n "$humidity" ] && echo "💧 ${humidity}%"; else echo "🌤️ --°"; fi'
    color = $sky
    font_size = 14
    font_family = $font
    position = 30, -35
    halign = left
    valign = top
    shadow_passes = 2
    shadow_size = 4
    shadow_color = rgba(137, 220, 235, 0.4)
}

# ==============================================================================
# SYSTEM STATUS
# ==============================================================================

# CPU Usage
label {
    monitor =
    text = cmd[update:2000] echo "CPU: $(top -bn1 | grep "Cpu(s)" | sed "s/.*, *\([0-9.]*\)%* id.*/\1/" | awk '{print 100 - $1}' | cut -d. -f1)%"
    color = $peach
    font_size = 12
    font_family = $font
    position = -30, -35
    halign = right
    valign = top
    shadow_passes = 1
    shadow_size = 2
    shadow_color = rgba(250, 179, 135, 0.3)
}

# RAM Usage
label {
    monitor =
    text = cmd[update:3000] free | grep Mem | awk '{printf "RAM: %.0f%%", $3/$2 * 100.0}'
    color = $yellow
    font_size = 12
    font_family = $font
    position = -30, -55
    halign = right
    valign = top
    shadow_passes = 1
    shadow_size = 2
    shadow_color = rgba(250, 227, 176, 0.3)
}


# ==============================================================================
# DECORATION
# ==============================================================================

# Diamond top left
label {
    monitor =
    text = ◇
    color = rgba(203, 166, 247, 0.4)
    font_size = 30
    font_family = $font
    position = -200, 200
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 6
    shadow_color = rgba(203, 166, 247, 0.2)
}

# Triangle top right
label {
    monitor =
    text = △
    color = rgba(245, 194, 231, 0.5)
    font_size = 25
    font_family = $font
    position = 200, 200
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 5
    shadow_color = rgba(245, 194, 231, 0.2)
}

# Hexagon bottom left
label {
    monitor =
    text = ⬢
    color = rgba(139, 213, 202, 0.4)
    font_size = 35
    font_family = $font
    position = -200, -250
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 7
    shadow_color = rgba(139, 213, 202, 0.2)
}

# Star bottom right
label {
    monitor =
    text = ✦
    color = rgba(250, 227, 176, 0.6)
    font_size = 28
    font_family = $font
    position = 200, -250
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 6
    shadow_color = rgba(250, 227, 176, 0.2)
}

# ==============================================================================
# AVATAR
# ==============================================================================

# Avatar outer glow
image {
    monitor =
    path = /home/Orion/Pictures/RandomIMGS/cb44d2345efa9d4b3cae136a9c124f92.jpg
    size = 80
    border_size = 1
    border_color = rgba(203, 166, 247, 0.2)
    position = 0, -8
    halign = center
    valign = center
    shadow_passes = 4
    shadow_size = 15
    shadow_color = rgba(203, 166, 247, 0.15)
}

# Avatar
image {
    monitor =
    path = /home/Orion/Pictures/RandomIMGS/cb44d2345efa9d4b3cae136a9c124f92.jpg
    size = 80
    border_size = 3
    border_color = $accent
    position = 0, -10
    halign = center
    valign = center
    shadow_passes = 3
    shadow_size = 8
    shadow_color = rgba(139, 125, 255, 0.5)
}





# ==============================================================================
# CORNER
# ==============================================================================

# Top left corner
label {
    monitor =
    text = ╭─
    color = rgba(245, 3, 3, 0.6)
    font_size = 20
    font_family = JetBrains Mono
    position = 15, -15
    halign = left
    valign = top
    shadow_passes = 2
    shadow_size = 4
    shadow_color = rgba(203, 166, 247, 0.3)
}

# Top right corner
label {
    monitor =
    text = ─╮
    color = rgba(245, 0, 0, 0.6)
    font_size = 20
    font_family = JetBrains Mono
    position = -15, -15
    halign = right
    valign = top
    shadow_passes = 2
    shadow_size = 4
    shadow_color = rgba(203, 166, 247, 0.3)
}

# Bottom left corner
label {
    monitor =
    text = ╰─
    color = rgba(245, 0, 0, 0.6)
    font_size = 20
    font_family = JetBrains Mono
    position = 15, 15
    halign = left
    valign = bottom
    shadow_passes = 2
    shadow_size = 4
    shadow_color = rgba(203, 166, 247, 0.3)
}

# Bottom right corner
label {
    monitor =
    text = ─╯
    color = rgba(245, 0, 0, 0.6)
    font_size = 20
    font_family = JetBrains Mono
    position = -15, 15
    halign = right
    valign = bottom
    shadow_passes = 2
    shadow_size = 4
    shadow_color = rgba(203, 166, 247, 0.3)
}

# ==============================================================================
# BOTTOM
# ==============================================================================

# System uptime - bottom left
label {
    monitor =
    text = cmd[update:60000] uptime -p | sed 's/up /⏱️ /'
    color = $green
    font_size = 12
    font_family = $font
    position = 30, 35
    halign = left
    valign = bottom
    shadow_passes = 2
    shadow_size = 3
    shadow_color = rgba(166, 227, 161, 0.4)
}

# Network status - bottom right
label {
    monitor =
    text = cmd[update:5000] timeout 1s bash -c 'if ping -c 1 -W 1 8.8.8.8 &>/dev/null; then echo "● Connected"; else echo "○ Offline"; fi'
    color = $blue
    font_size = 12
    font_family = $font
    position = -30, 35
    halign = right
    valign = bottom
    shadow_passes = 2
    shadow_size = 3
    shadow_color = rgba(137, 220, 235, 0.4)
}
# ==============================================================================
# WELCOME MESSAGE
# ==============================================================================

label {
    monitor =
    text = Welcome back, Imperator
    color = $text
    font_size = 18
    font_family = $font
    position = 0, -80
    halign = center
    valign = center
    shadow_passes = 2
    shadow_size = 3
    shadow_color = rgba(241, 1, 1, 0.3)
}
# div {
# @define-color foreground #9ec7c8;
# @define-color background #010203;
# @define-color cursor #9ec7c8;

# @define-color color0 #010203;
# @define-color color1 #265062;
# @define-color color2 #605F50;
# @define-color color3 #9B6A45;
# @define-color color4 #758473;
# @define-color color5 #BD9B56;
# @define-color color6 #327588;
# @define-color color7 #9ec7c8;
# @define-color color8 #6e8b8c;
# @define-color color9 #265062;
# @define-color color10 #605F50;
# @define-color color11 #9B6A45;
# @define-color color12 #758473;
# @define-color color13 #BD9B56;
# @define-color color14 #327588;
@define-color color15 #bb3a3aff;
# }